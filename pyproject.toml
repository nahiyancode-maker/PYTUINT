[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "PYTUINT"
version = "0.1.0"
description = "A Python TUI library"
requires-python = ">=3.8"